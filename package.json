{
  "name": "groceries",
  "version": "1.0.0",
  "private": true,
  "description": "Household grocery inventory and virtual fridge display.",
  "scripts": {
    "test": "node --check virtual-fridge/app.js && node scripts/verify-virtual-fridge.mjs"
  }
}
