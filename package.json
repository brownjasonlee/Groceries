{
  "name": "groceries",
  "version": "1.0.0",
  "private": true,
  "description": "Household grocery inventory and virtual fridge display.",
  "scripts": {
    "receipts": "node scripts/verify-receipts.mjs && node scripts/render-receipts.mjs",
    "test": "node --check virtual-fridge/app.js && node scripts/verify-virtual-fridge.mjs && npm run receipts"
  }
}
